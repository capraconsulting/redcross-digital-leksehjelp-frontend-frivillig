import React from 'react';
import Cross from '../assets/Cross';
/*
interface IProps<T> {
  text: string;
  onRemove(object: Type1 | type2): void;
  id: number;
}

const TagComponent = <T extends any>({ text, onRemove }: IProps<T>) => (
  <div className="subject--list-element" >
    <p>{text}</p>
    <button
      className="leksehjelp--button-close"
      onClick={e => onRemove({ id, subject, type, e })}
    >
      <Cross color="#8b51c6" />
    </button>
  </div >
)
export default TagComponent;
*/